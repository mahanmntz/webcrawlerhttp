# راهنمای جامع معماری، چرخه داده و دستورات کراولر توزیع‌شده
### Distributed Enterprise Web Crawler & Scraper Monorepo (Alex Xu - Chapter 9)

این سند مرجع کامل شما برای درک چرایی معماری، پشت صحنه‌ی تک‌تک دستورات، جریان حرکت داده‌ها و نحوه استفاده پروداکشنی از این پروژه است.

---

## ۱. ما اصلاً داریم چیکار می‌کنیم؟ (دید کلان از ارتفاع ۳۰ هزار پایی)

تصور کنید می‌خواهید مثل موتور جستجوی گوگل یا شرکت‌های تحلیل کلان‌داده، میلیون‌ها صفحه وب را در کوتاه‌ترین زمان دانلود کنید، اطلاعات مفیدشان (تیتر، متن، لینک‌ها) را استخراج کنید و در دیتابیس ذخیره نمایید.

### چالش‌های بزرگ مهندسی:
1. **چرخه‌های بی‌نهایت و صفحات تکراری (Infinite Loops & Traps):**
   اگر وب‌سایتی لینک‌های خود را در چرخه قرار داده باشد (صفحه A به B و B به A)، کراولر بدون کنترل هوشمند تا ابد درجا می‌زند و رم سرور را می‌بلعد.
2. **کشتن سرور مقصد (DDoS ناخواسته):**
   اگر ۱۰۰ ورکر همزمان به یک سایت کوچک در یک ثانیه ریکوئست بفرستند، سرور آن سایت از کار می‌افتد یا آی‌پی شما را بلاک (Ban) می‌کند. کراولر باید **مودب (Polite)** باشد.
3. **گلوگاه پردازشی در برابر گلوگاه شبکه (I/O-Bound vs CPU-Bound):**
   دانلود کردن HTML با اینترنت، منتظر شبکه ماندن است. اما کالبدشکافی کدهای HTML و بررسی سلکتورهای CSS شدیداً پردازنده (CPU) مصرف می‌کند. یک سیستم تک‌سرویسی نمی‌تواند هر دو را با حداکثر توان انجام دهد.

---

## ۲. چرا این ترکیب فناوری‌ها (Go + Python + TypeScript + Redis)؟

ما یک سیستم **Polyglot (چندزبانه)** با تفکیک دقیق وظایف (Separation of Concerns) ساختیم:

| تکنولوژی | نقش در سیستم | چرا این انتخاب؟ |
| :--- | :--- | :--- |
| **Redis 7.2** | ستون فقرات توزیع‌شده (State & Broker) | انجام عملیات اتمیک (Atomic) در کسر میلی‌ثانیه؛ مدیریت صف‌ها (`LPUSH/BRPOPLPUSH`)، بررسی بلادرنگ آدرس‌های تکراری (`SADD`) و قفل ادب دامنه (`SET NX PX`). |
| **Go (Golang)** | موتور پرسرعت دانلود (Crawler Engine) | مصرف رم فوق‌العاده ناچیز به ازای هر اتصال؛ پیاده‌سازی Worker Pool همروند بدون خطر کرش ناشی از باز ماندن سوکت‌ها (`EMFILE`). |
| **Python** | پارسر و کالبدشکافی محتوا (Parser & Scraper) | داشتن قوی‌ترین و پایدارترین اکوسیستم استخراج متن و پردازش زبان و DOM (کتابخانه‌ی قدرتمند BeautifulSoup) و ولیدیشن دقیق با Pydantic. |
| **Node.js / TS** | ارکستراسیون و API Gateway (Fastify) | وب‌سرور مدرن، بسیار سریع و Type-Safe برای دریافت درخواست‌های کاربر، اعتبارسنجی اولیه آدرس‌ها و مانیتورینگ متریک‌های کلاستر. |

---

## ۳. سفر یک URL از ابتدا تا انتها (The End-to-End Life Cycle)

```
 [کاربر / کاربر نهایی]
         │
         ▼  (۱) ارسال درخواست: POST /api/jobs
 [API Gateway - Fastify]
         │
         ▼  (۲) بررسی تکرار در Redis: SADD frontier:seen
    ┌────┴────────────────────────┐
 [قبلاً دیده شده]           [کاملاً جدید است]
    │                             │
  (رد ۴۰۹)                        ▼  (۳) ایجاد شناسه UUID و قرارداد CrawlTarget
                            [صف frontier:queue]
                                  │
                                  ▼  (۴) برداشت مطمئن: BRPOPLPUSH
                          [Crawler Engine - Go]
                                  │
                   ┌──────────────┴──────────────┐
             [دامنه داغ است]               [دامنه خنک است]
                   │                             │
         (بازگشت موقت به صف)             (۵) دانلود HTTP با ترانسپورت بهینه
                                                 │
                                                 ▼  (۶) تولید قرارداد RawPage
                                         [صف queue:raw_pages]
                                                 │
                                                 ▼  (۷) خواندن با BRPOP
                                      [Parser Worker - Python]
                                                 │
                                                 ▼  (۸) پارس DOM و استخراج تیتر و لینک‌ها
                                         [صف queue:parsed_docs] ──► (ذخیره در دیتابیس)
                                                 │
                                                 ▼  (۹) بستن چرخه: تزریق لینک‌های نو
                                         [صف frontier:queue] ──► (برای گام بعدی Go!)
```

---

## ۴. کالبدشکافی تک‌تک دستورات Makefile (دستورات از کجا میان و چیکار می‌کنن؟)

تمام دستوراتی که اجرا می‌کنید در فایل [Makefile](file:///Users/mahan/development/backend-pr/webcrawlerhttp/Makefile) نوشته شده‌اند. بیایید پشت صحنه هر دستور را دقیق بشکافیم:

---

### ۱) `make cluster-up`
* **دستور واقعی پشت صحنه:** `docker compose up -d --build`
* **چیکار می‌کنه؟** فایل [docker-compose.yml](file:///Users/mahan/development/backend-pr/webcrawlerhttp/docker-compose.yml) را می‌خواند؛ ۴ کانتینر داکر را می‌سازد (Build) و در پس‌زمینه اجرا می‌کند:
  1. کانتینر سرور ردیس (`crawler-redis`)
  2. کانتینر گیت‌وی نودجی‌اس (`crawler-api-gateway`)
  3. کانتینر موتور کراولر گو (`crawler-engine-worker`)
  4. کانتینر پارسر پایتون (`crawler-parser-scraper`)
* **کی استفاده کنیم؟** وقتی می‌خواهید کل سیستم توزیع‌شده با تمام اجزا بالا بیاید و شروع به کار کند.

---

### ۲) `make submit-job URL=https://example.com`
* **دستور واقعی پشت صحنه:**
  ```bash
  curl -X POST http://localhost:3000/api/jobs \
    -H "Content-Type: application/json" \
    -d '{"url":"https://example.com","max_depth":2,"priority":5}'
  ```
* **چیکار می‌کنه؟** به API Gateway روی پورت ۳۰۰۰ درخواست می‌زند. Fastify آدرس را چک می‌کند، بررسی می‌کند که آیا آدرس تکراری است یا خیر (`SADD frontier:seen`). اگر جدید باشد، آن را بسته‌بندی کرده و داخل صف ردیس می‌اندازد.
* **کی استفاده کنیم؟** هر بار که می‌خواهید یک سایت ریشه (Seed URL) جدید به کراولر بدهید تا شروع به شخم زدن آن کند!

---

### ۳) `make get-metrics`
* **دستور واقعی پشت صحنه:**
  ```bash
  curl -s http://localhost:3000/api/metrics
  ```
* **چیکار می‌کنه؟** با پایپ‌لاین ردیس وضعیت تمام صف‌ها را می‌خواند و این متریک‌ها را برمی‌گرداند:
  - `pending_queue`: تعداد آدرس‌هایی که در صف منتظر دانلود توسط Go هستند.
  - `in_flight_processing`: تعداد صفحاتی که دقیقاً همین لحظه ورکرها در حال دانلودشان هستند.
  - `raw_pages_for_parser`: تعداد صفحات دانلودی که در صف منتظر پایتون هستند.
  - `parsed_documents_total`: تعداد کل صفحاتی که تا این لحظه پارس شده‌اند.
  - `unique_urls_seen`: کل آدرس‌های یکتایی که تا به حال سیستم دیده است.
* **کی استفاده کنیم؟** برای مانیتورینگ سلامت، نرخ پردازش و دیدن حجم کارهای باقیمانده در کلاستر.

---

### ۴) `make get-docs`
* **دستور واقعی پشت صحنه:**
  ```bash
  curl -s http://localhost:3000/api/documents
  ```
* **چیکار می‌کنه؟** اسناد ساختاریافته‌ی نهایی را که پایتون استخراج کرده (شامل عنوان صفحه، توضیحات متا، نمونه متن بدون تگ‌های اضافی و تمام لینک‌های استخراج شده) از صف `queue:parsed_docs` می‌خواند و نمایش می‌دهد.
* **کی استفاده کنیم؟** برای دیدن دیتای تمیز و مفیدی که استخراج شده و آماده‌ی ذخیره در دیتابیس (Elasticsearch, PostgreSQL, MongoDB) است.

---

### ۵) `make test-all`
* **دستور واقعی پشت صحنه:**
  اجرای همزمان:
  1. `go test -v ./services/crawler-engine/...` (تست‌های واحد Go)
  2. `pytest -v services/parser-scraper/tests` (تست‌های واحد Python)
  3. `vitest run` در فولدر `services/api-gateway` (تست‌های واحد TypeScript)
* **چیکار می‌کنه؟** در کمتر از ۲ ثانیه، منطق حیاتی هر ۳ زبان (پارس HTML، تایم‌اوت شبکه، ولیدیشن اسکیمای API) را تست می‌کند و اگر کدی شکسته باشد فوراً هشدار می‌دهد.
* **کی استفاده کنیم؟** قبل از هر کامیت یا هنگام اضافه کردن قابلیت‌های جدید (CI/CD Pipeline).

---

### ۶) `make cluster-logs`
* **دستور واقعی پشت صحنه:** `docker compose logs -f`
* **چیکار می‌کنه؟** ترمینال را به لاگ‌های زنده کانتینرها متصل می‌کند تا ببینید هر ورکر در هر لحظه در حال انجام چه کاری است.

---

### ۷) `make cluster-down`
* **دستور واقعی پشت صحنه:** `docker compose down`
* **چیکار می‌کنه؟** تمامی ۴ کانتینر را به آرامی متوقف کرده، منابع سیستم و شبکه را آزاد می‌کند، در حالی که داده‌های ردیس در ولوم `redis_data` سالم باقی می‌مانند.

---

## ۵. چگونه از این سیستم در دنیای واقعی استفاده کنیم؟

این پروژه ساختار آماده‌ی استقرار روی سرور پروداکشن (مثلاً Kubernetes یا Docker Swarm) را دارد:

1. **برای کراول سایت‌های خبری / وبلاگ‌ها:**
   کافیست یک اسکریپت یا کران‌جاب (Cron Job) روزی یک بار دستور `make submit-job URL=...` را صدا بزند.
2. **اتصال به دیتابیس دائمی:**
   می‌توان در فایل [pipeline.py](file:///Users/mahan/development/backend-pr/webcrawlerhttp/services/parser-scraper/app/pipeline.py) خط ۳۲ را تغییر داد تا به جای ذخیره در ردیس موقت، آبجکت `parsed_doc` را مستقیماً در PostgreSQL یا Elasticsearch ذخیره کند.
3. **مقیاس‌پذیری ورکرها (Scaling):**
   اگر حجم صفحات زیاد شد، در داکر با دستور `docker compose up -d --scale crawler-engine=3 --scale parser-scraper=4` می‌توانید ظرفیت موتورها را چند برابر کنید بدون اینکه کدی دستکاری شود!
